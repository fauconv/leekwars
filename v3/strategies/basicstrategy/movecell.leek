/******************************************************************************
 * MoveCell
 ******************************************************************************/
class MoveCell extends Objects {
  
  integer ownerId;
  Cell cell;
  Array<Simulation> simulations;
  Array<integer> MPRemainingBySimulation;
  integer maxMPRemaining;
  Array<RelativeCell> targetCells = []; // liste de cell que l on peut viser pour atteindre l ennemie a partir de cette moveCell
  Map<real,Map<integer,Tool>> enemyTools = [:]; // liste des tools enemy qui peuvent nous atteindre sur cette moveCell classe par AOE
  Map<integer,MoveCell> escapes = [:]; // liste des cell vers lesquels je peux fuir apres avoir agis ici
  Map<integer,MoveCell> goodEscapes = [:]; // liste des cell vers lesquels je peux fuir apres avoir agis ici et qui me permette de pas etre touche
	
  /**
   * constructor
   * Attention: simulations et MPRemainingBySimulation doivent etre de la meme taille
   **/
  constructor(Cell cell, integer ownerId, Array<Simulation> simulations, Array<integer> MPRemainingBySimulation, integer maxMPRemaining) {
    id = cell.id;
    this.cell = cell;
    this.ownerId = ownerId;
	this.simulations = simulations;
	this.MPRemainingBySimulation = MPRemainingBySimulation;
  }  
	
}
